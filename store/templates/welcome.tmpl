<img src="images/{{#setting}}{{brandCode}}{{/setting}}/logo.png" class="logo-img" width=350>
<div class="buttons">
    {{#if settings.welcomeScreenWesURL}}
	    {{#unless is_subscriber}}
	        <button class="btn print-subscriber">{{../settings.welcomePrintSubsBtn}}</button>
	    {{/unless}}
    {{/if}}
    {{#unless is_subscriber}}
        <button class="btn subscribe">{{../settings.welcomeSubscribeBtn}}</button>
    {{/unless}}
    <a class="goto-store">{{settings.welcomeBrowseBtn}}</a>
</div>

{{!-- 
{{#unless is_logged_in}}
  <div class='already-have-account'>{{../settings.welcomeAlreadyHaveAcct}}</div>
{{/unless}}
--}}
