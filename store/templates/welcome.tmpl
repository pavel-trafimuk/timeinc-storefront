<img class='cover-img-only' src="{{img_only_cover_url}}" width="100%">
<img class='cover-with-text' src="{{full_cover_url}}" width="100%">
<div class="buttons">
    <button class="reload-page">Reload</button>
    <button class="open-preview">Preview</button>
    <button class="goto-store">Browse Issues</button>
    <button class="launch-repl">REPL</button>
</div>

<div class='already-have-account'>Already have an account?</div>
